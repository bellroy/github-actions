name: Test String Needs
on:
  push:
    branches-ignore:
      - "refs/tags/*_staging"
      - "refs/tags/*_production"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "build"
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "test"
